name: Simplified Model Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Hello, this is a test job!"
